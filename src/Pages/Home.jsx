import React, { use } from 'react';
import AuthContext from '../Context/AuthContext/AuthContext';

const Home = () => {
    
        return (
        <div>
                <h1>This is home</h1>

        </div>
    );
};

export default Home;